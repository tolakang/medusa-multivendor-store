{
  "name": "@medusa/backend",
  "version": "1.0.0",
  "private": true,
  "description": "Medusa 2.0 multi-vendor e-commerce backend",
  "scripts": {
    "dev": "medusa develop",
    "build": "medusa build",
    "start": "cd .medusa/server && medusa start",
    "predeploy": "medusa db:migrate && medusa links:sync",
    "migrate": "medusa db:migrate",
    "seed": "medusa seed",
    "test": "jest"
  },
  "dependencies": {
    "@medusajs/medusa": "^2.0.0",
    "@medusajs/cli": "^2.0.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
